import { TransofrmInterceptor } from './transofrm.interceptor';

describe('TransofrmInterceptor', () => {
  it('should be defined', () => {
    expect(new TransofrmInterceptor()).toBeDefined();
  });
});
